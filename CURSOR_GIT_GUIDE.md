# 🎯 Работа с Git в Cursor IDE

## ✅ Проект подключен к GitHub!

Ваш репозиторий: **https://github.com/JorniJave/Baraholka**

## 🔧 Встроенная поддержка Git в Cursor

Cursor IDE имеет **полную встроенную поддержку Git** - вам не нужно ничего дополнительно настраивать!

### 📍 Где найти Git функции в Cursor:

1. **Боковая панель Source Control:**
   - Нажмите на иконку **Source Control** (ветка) в левой панели
   - Или используйте горячую клавишу: `Ctrl+Shift+G` (Windows/Linux) или `Cmd+Shift+G` (Mac)

2. **Статус файлов:**
   - Измененные файлы отображаются с буквой **M** (Modified)
   - Новые файлы с буквой **U** (Untracked)
   - Удаленные файлы с буквой **D** (Deleted)

3. **Индикатор в статус-баре:**
   - Внизу экрана видна текущая ветка (например: `main`)
   - Количество изменений (например: `3 changes`)

## 🚀 Основные операции в Cursor:

### 1. **Просмотр изменений:**
   - Откройте Source Control панель
   - Кликните на файл, чтобы увидеть diff (различия)

### 2. **Добавить файлы в коммит:**
   - Нажмите **+** рядом с файлом (Stage Changes)
   - Или нажмите **+** рядом с "Changes" чтобы добавить все файлы

### 3. **Создать коммит:**
   - Введите сообщение коммита в поле сверху
   - Нажмите **✓ Commit** (или `Ctrl+Enter`)
   - Или используйте кнопку **Commit & Push** для отправки сразу

### 4. **Отправить на GitHub (Push):**
   - Нажмите на **...** (три точки) в Source Control панели
   - Выберите **Push**
   - Или используйте кнопку **Sync Changes** (синхронизирует pull + push)

### 5. **Получить изменения с GitHub (Pull):**
   - Нажмите на **...** (три точки)
   - Выберите **Pull**
   - Или используйте **Sync Changes**

## ⌨️ Горячие клавиши:

| Действие | Windows/Linux | Mac |
|----------|---------------|-----|
| Открыть Source Control | `Ctrl+Shift+G` | `Cmd+Shift+G` |
| Создать коммит | `Ctrl+Enter` | `Cmd+Enter` |
| Показать Git лог | `Ctrl+Shift+P` → "Git: View History" | `Cmd+Shift+P` → "Git: View History" |

## 📋 Полезные команды через Command Palette:

Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac) и введите:

- **Git: Clone** - клонировать репозиторий
- **Git: Pull** - получить изменения
- **Git: Push** - отправить изменения
- **Git: Commit** - создать коммит
- **Git: Show Output** - показать вывод Git команд
- **Git: View History** - просмотреть историю коммитов

## 🔐 Аутентификация в GitHub:

Если при push/pull запрашивается авторизация:

1. **Через браузер (рекомендуется):**
   - Cursor откроет браузер для авторизации
   - Войдите в свой GitHub аккаунт
   - Разрешите доступ

2. **Через Personal Access Token:**
   - Создайте токен на GitHub: Settings → Developer settings → Personal access tokens
   - Используйте токен вместо пароля

## 📝 Рекомендуемый workflow:

1. **Перед началом работы:**
   ```
   Pull изменения с GitHub (если работаете в команде)
   ```

2. **Во время работы:**
   - Делайте изменения в коде
   - Cursor автоматически отслеживает изменения

3. **После завершения работы:**
   ```
   1. Откройте Source Control (Ctrl+Shift+G)
   2. Просмотрите изменения
   3. Добавьте файлы в staging (+)
   4. Напишите сообщение коммита
   5. Нажмите Commit & Push
   ```

## 🎨 Визуальные индикаторы:

- **Зеленая полоса слева** - файл добавлен в staging
- **Синяя полоса слева** - файл изменен, но не в staging
- **Серая полоса слева** - файл игнорируется (.gitignore)

## ⚠️ Важные моменты:

1. **`.env` файл не попадет в репозиторий** - он в `.gitignore` ✅
2. **Базы данных и логи** также исключены ✅
3. **Всегда проверяйте изменения** перед коммитом
4. **Пишите понятные сообщения коммитов**

## 🆘 Если что-то пошло не так:

### Отменить изменения в файле:
- Правый клик на файл → **Discard Changes**

### Отменить последний коммит (локально):
```bash
git reset --soft HEAD~1
```

### Посмотреть историю:
- Command Palette → "Git: View History"
- Или в терминале: `git log`

---

**Готово!** Теперь вы можете работать с Git прямо в Cursor IDE! 🎉

