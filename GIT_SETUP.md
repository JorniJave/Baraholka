# 📦 Подключение проекта к Git

## ✅ Что уже сделано:
- ✅ Git репозиторий инициализирован
- ✅ Создан `.gitignore` файл (исключает `.env`, базы данных, логи и т.д.)
- ✅ Создан первый коммит со всеми файлами проекта

## 🔗 Подключение к удаленному репозиторию (GitHub/GitLab)

### Вариант 1: Создать новый репозиторий на GitHub

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Введите название репозитория (например: `baraholka-bot`)
   - Выберите **Private** (приватный) или **Public** (публичный)
   - **НЕ** создавайте README, .gitignore или лицензию (они уже есть)
   - Нажмите **Create repository**

2. **Подключите локальный репозиторий к удаленному:**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/baraholka-bot.git
   ```

3. **Отправьте код на GitHub:**
   ```bash
   git branch -M main
   git push -u origin main
   ```

### Вариант 2: Подключить к существующему репозиторию

Если у вас уже есть репозиторий на GitHub/GitLab:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git branch -M main
git push -u origin main
```

## 📝 Основные команды Git

### Проверить статус:
```bash
git status
```

### Добавить изменения:
```bash
git add .
# или конкретный файл:
git add bot/config.py
```

### Создать коммит:
```bash
git commit -m "Описание изменений"
```

### Отправить изменения на GitHub:
```bash
git push
```

### Получить изменения с GitHub:
```bash
git pull
```

### Посмотреть историю коммитов:
```bash
git log
```

## ⚠️ Важно!

- Файл `.env` с токенами и секретами **НЕ** попадет в репозиторий (благодаря `.gitignore`)
- Базы данных (`.db` файлы) также исключены
- Логи (`.log` файлы) исключены

## 🔐 Если нужно добавить .env.example

Создайте файл `bot/.env.example` с примером конфигурации (без реальных токенов):

```env
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321
CHANNEL_ID=-1001234567890
```

Этот файл можно безопасно коммитить в репозиторий.

