# 🐧 Развертывание бота на Linux

Подробная инструкция по развертыванию Telegram бота для барахолки на Linux сервере.

## 📋 Требования

- Linux сервер (Ubuntu/Debian/CentOS)
- Python 3.10 или выше
- Права суперпользователя (sudo)
- Git

## 🚀 Пошаговая инструкция

### 1. Подготовка сервера

#### Обновление системы (Ubuntu/Debian)

```bash
sudo apt update
sudo apt upgrade -y
```

#### Установка Python и необходимых пакетов

```bash
# Ubuntu/Debian
sudo apt install -y python3 python3-pip python3-venv git

# Проверка версии Python (должна быть 3.10+)
python3 --version
```

### 2. Создание пользователя для бота (рекомендуется)

```bash
# Создать пользователя
sudo useradd -m -s /bin/bash baraholka

# Переключиться на пользователя
sudo su - baraholka
```

### 3. Клонирование репозитория

```bash
# Перейти в домашнюю директорию
cd ~

# Клонировать репозиторий
git clone https://github.com/JorniJave/Baraholka.git

# Перейти в директорию проекта
cd Baraholka/bot
```

### 4. Настройка виртуального окружения

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate

# Обновить pip
pip install --upgrade pip
```

### 5. Установка зависимостей

```bash
# Установить зависимости из requirements.txt
pip install -r requirements.txt
```

### 6. Настройка переменных окружения

```bash
# Создать файл .env
nano .env
```

Добавьте в файл:

```env
# Токен бота от @BotFather
BOT_TOKEN=your_bot_token_here

# ID администраторов (через запятую, без пробелов)
ADMIN_IDS=123456789,987654321

# ID канала для публикации объявлений (начинается с -100)
CHANNEL_ID=-1001234567890
```

Сохраните файл: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

**Важно:** Убедитесь, что файл `.env` имеет правильные права доступа:

```bash
chmod 600 .env
```

### 7. Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запустите бота
python3 bot.py
```

Если бот запустился успешно, остановите его: `Ctrl+C`

### 8. Настройка systemd для автозапуска

#### Создание systemd сервиса

```bash
# Выйти из пользователя baraholka (если использовали)
exit

# Создать файл сервиса
sudo nano /etc/systemd/system/baraholka-bot.service
```

Добавьте следующее содержимое (замените пути на ваши):

```ini
[Unit]
Description=Baraholka Telegram Bot
After=network.target

[Service]
Type=simple
User=baraholka
Group=baraholka
WorkingDirectory=/home/baraholka/Baraholka/bot
Environment="PATH=/home/baraholka/Baraholka/bot/venv/bin"
ExecStart=/home/baraholka/Baraholka/bot/venv/bin/python3 bot.py
Restart=always
RestartSec=10
StandardOutput=append:/home/baraholka/Baraholka/bot/baraholka.log
StandardError=append:/home/baraholka/Baraholka/bot/baraholka.log

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `/home/baraholka` на путь к вашему пользователю, если используете другого.

#### Активация сервиса

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск при загрузке системы
sudo systemctl enable baraholka-bot.service

# Запустить сервис
sudo systemctl start baraholka-bot.service

# Проверить статус
sudo systemctl status baraholka-bot.service
```

### 9. Управление ботом

#### Просмотр статуса

```bash
sudo systemctl status baraholka-bot.service
```

#### Остановка бота

```bash
sudo systemctl stop baraholka-bot.service
```

#### Запуск бота

```bash
sudo systemctl start baraholka-bot.service
```

#### Перезапуск бота

```bash
sudo systemctl restart baraholka-bot.service
```

#### Просмотр логов

```bash
# Логи systemd (последние 50 строк)
sudo journalctl -u baraholka-bot.service -n 50

# Логи в реальном времени
sudo journalctl -u baraholka-bot.service -f

# Логи из файла
tail -f /home/baraholka/Baraholka/bot/baraholka.log
```

### 10. Настройка firewall (если используется)

Если на сервере включен firewall, убедитесь, что он не блокирует исходящие соединения:

```bash
# Для UFW (Ubuntu)
sudo ufw allow out 443/tcp
sudo ufw allow out 80/tcp

# Для firewalld (CentOS/RHEL)
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --reload
```

## 🔧 Дополнительные настройки

### Обновление бота

```bash
# Переключиться на пользователя бота
sudo su - baraholka

# Перейти в директорию проекта
cd Baraholka

# Остановить бота
sudo systemctl stop baraholka-bot.service

# Получить обновления
git pull origin main

# Активировать виртуальное окружение
cd bot
source venv/bin/activate

# Обновить зависимости (если изменились)
pip install -r requirements.txt

# Запустить бота
sudo systemctl start baraholka-bot.service

# Проверить статус
sudo systemctl status baraholka-bot.service
```

### Резервное копирование базы данных

```bash
# Создать скрипт для бэкапа
nano ~/backup_bot.sh
```

Добавьте:

```bash
#!/bin/bash
BACKUP_DIR="/home/baraholka/backups"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
cp /home/baraholka/Baraholka/bot/baraholka.db $BACKUP_DIR/baraholka_$DATE.db
# Удалить бэкапы старше 7 дней
find $BACKUP_DIR -name "baraholka_*.db" -mtime +7 -delete
```

Сделайте скрипт исполняемым:

```bash
chmod +x ~/backup_bot.sh
```

Добавьте в crontab для автоматического бэкапа:

```bash
crontab -e
```

Добавьте строку (бэкап каждый день в 3:00):

```
0 3 * * * /home/baraholka/backup_bot.sh
```

### Мониторинг использования ресурсов

```bash
# Просмотр использования памяти и CPU
ps aux | grep bot.py

# Мониторинг в реальном времени
top -p $(pgrep -f bot.py)
```

## 🐛 Решение проблем

### Бот не запускается

1. **Проверьте логи:**
   ```bash
   sudo journalctl -u baraholka-bot.service -n 100
   ```

2. **Проверьте права доступа:**
   ```bash
   ls -la /home/baraholka/Baraholka/bot/
   ```

3. **Проверьте файл .env:**
   ```bash
   cat /home/baraholka/Baraholka/bot/.env
   ```

4. **Проверьте виртуальное окружение:**
   ```bash
   source /home/baraholka/Baraholka/bot/venv/bin/activate
   python3 --version
   ```

### Ошибка "Permission denied"

```bash
# Установить правильные права
sudo chown -R baraholka:baraholka /home/baraholka/Baraholka
chmod 600 /home/baraholka/Baraholka/bot/.env
```

### Бот постоянно перезапускается

1. Проверьте логи на наличие ошибок:
   ```bash
   sudo journalctl -u baraholka-bot.service -f
   ```

2. Проверьте, не запущено ли несколько экземпляров:
   ```bash
   ps aux | grep bot.py
   ```

3. Убедитесь, что в `.env` указаны правильные значения

### Проблемы с базой данных

```bash
# Проверьте права на файл БД
ls -la /home/baraholka/Baraholka/bot/baraholka.db

# Если нужно, пересоздайте БД (ВНИМАНИЕ: удалит все данные!)
sudo systemctl stop baraholka-bot.service
rm /home/baraholka/Baraholka/bot/baraholka.db
sudo systemctl start baraholka-bot.service
```

## 📊 Мониторинг и обслуживание

### Просмотр статистики использования

```bash
# Использование диска
df -h

# Использование памяти
free -h

# Процессы бота
ps aux | grep bot.py
```

### Очистка логов

```bash
# Очистить логи systemd (старые записи)
sudo journalctl --vacuum-time=30d

# Очистить файл логов бота (если он большой)
> /home/baraholka/Baraholka/bot/baraholka.log
```

## 🔐 Безопасность

### Рекомендации:

1. **Используйте отдельного пользователя** для запуска бота (не root)
2. **Ограничьте права доступа** к файлу `.env` (600)
3. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
4. **Используйте SSH ключи** вместо паролей
5. **Настройте firewall** для ограничения доступа

## 📝 Чек-лист развертывания

- [ ] Python 3.10+ установлен
- [ ] Репозиторий склонирован
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены
- [ ] Файл `.env` создан и настроен
- [ ] Тестовый запуск успешен
- [ ] systemd сервис создан и настроен
- [ ] Бот запускается автоматически
- [ ] Логи проверены, ошибок нет
- [ ] Резервное копирование настроено (опционально)

## 🆘 Получение помощи

Если возникли проблемы:

1. Проверьте логи: `sudo journalctl -u baraholka-bot.service -n 100`
2. Проверьте файл логов: `tail -f /home/baraholka/Baraholka/bot/baraholka.log`
3. Убедитесь, что все зависимости установлены
4. Проверьте правильность настроек в `.env`

---

**Готово!** Бот должен работать на вашем Linux сервере. 🎉

